# File mẫu cho Streamlit secrets.
# KHÔNG đổi tên file này thành secrets.toml rồi commit lên GitHub.
# Nếu sau này app cần token/API key, nhập trực tiếp trong:
# Streamlit Community Cloud -> Manage app -> Settings -> Secrets

# FIREANT_TOKEN = ""
# SIMPLIZE_TOKEN = ""
